/**
 * Shared brand font style objects.
 *
 * Prefer the Tailwind utilities when possible:
 *   serif  → font-serif-display   (Cormorant Garamond)
 *   sans   → font-sans-label      (Montserrat)
 *
 * Use these JS objects only when an inline `style` prop is required
 * (e.g. inside framer-motion components or third-party wrappers).
 */
export const serif = { fontFamily: "'Cormorant Garamond', Georgia, serif" };
export const sans  = { fontFamily: "'Montserrat', sans-serif" };
